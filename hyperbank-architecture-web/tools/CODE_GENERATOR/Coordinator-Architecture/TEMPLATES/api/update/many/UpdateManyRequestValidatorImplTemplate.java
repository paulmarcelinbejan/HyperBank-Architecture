package ${PACKAGE}.${ENTITY_LOWERCASE}.api.update.many;

import io.github.paulmarcelinbejan.toolbox.utils.validation.stereotype.Validator;

@Validator
public class UpdateManyRequest${ENTITY}ValidatorImpl implements UpdateManyRequest${ENTITY}Validator {
	
}
