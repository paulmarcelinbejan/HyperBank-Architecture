package ${PACKAGE}.${ENTITY_LOWERCASE}.api.update.one;

import io.github.paulmarcelinbejan.toolbox.utils.validation.stereotype.Validator;

@Validator
public class ${ENTITY}UpdateRequestValidatorImpl implements ${ENTITY}UpdateRequestValidator {
	
}
