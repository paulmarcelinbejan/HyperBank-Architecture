package ${PACKAGE}.${ENTITY_LOWERCASE}.api.update.one;

import ${PACKAGE}.${ENTITY_LOWERCASE}.dto.${ENTITY}UpdateRequest;

import io.github.paulmarcelinbejan.coordinator.architecture.validator.jakarta.JakartaBaseValidator;

public interface ${ENTITY}UpdateRequestValidator extends JakartaBaseValidator<${ENTITY}UpdateRequest> {
	
}
