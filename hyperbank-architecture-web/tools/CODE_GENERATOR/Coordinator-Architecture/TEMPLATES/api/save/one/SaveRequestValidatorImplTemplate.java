package ${PACKAGE}.${ENTITY_LOWERCASE}.api.save.one;

import io.github.paulmarcelinbejan.toolbox.utils.validation.stereotype.Validator;

@Validator
public class ${ENTITY}SaveRequestValidatorImpl implements ${ENTITY}SaveRequestValidator {
	
}
