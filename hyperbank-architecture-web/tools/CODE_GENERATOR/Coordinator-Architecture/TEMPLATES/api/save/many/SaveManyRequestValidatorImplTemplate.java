package ${PACKAGE}.${ENTITY_LOWERCASE}.api.save.many;

import io.github.paulmarcelinbejan.toolbox.utils.validation.stereotype.Validator;

@Validator
public class ${ENTITY}SaveRequestsValidatorImpl implements ${ENTITY}SaveRequestsValidator {
	
}
